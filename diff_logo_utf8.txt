diff --git a/src/components/AovaLogo3D.tsx b/src/components/AovaLogo3D.tsx
index 2cec8ae..21a6d47 100644
--- a/src/components/AovaLogo3D.tsx
+++ b/src/components/AovaLogo3D.tsx
@@ -1,38 +1,38 @@
 "use client";
 
 import { useRef, useMemo, useEffect, useState } from "react";
-import { Canvas, useFrame, useThree } from "@react-three/fiber";
-import { Float } from "@react-three/drei";
+import { Canvas, useThree } from "@react-three/fiber";
 import * as THREE from "three";
 import { SVGLoader } from "three-stdlib";
 
 const LOGO_PATH =
     "M171.27,0v91c0,44.33-35.94,80.27-80.27,80.27h-31.96v-59.04h59.04v-59.04h-59.04v59.04H0v-31.96C0,35.94,35.94,0,80.27,0h91Z";
 
-/* ÔöÇÔöÇÔöÇ Procedural env map: bright multi-colour gradient for rich reflections ÔöÇ */
+/* ÔöÇÔöÇÔöÇ Procedural env map: neutral studio HDRI for polished chrome reflections ÔöÇ */
 function useProceduralEnv() {
     const { gl } = useThree();
     return useMemo(() => {
         const pmrem = new THREE.PMREMGenerator(gl);
         pmrem.compileEquirectangularShader();
-        const size = 64;
+        const size = 128;
         const data = new Uint8Array(size * size * 4);
         for (let y = 0; y < size; y++) {
             for (let x = 0; x < size; x++) {
-                const tx = x / size;   // 0Ôćĺ1 horizontal
-                const ty = y / size;   // 0Ôćĺ1 vertical
+                const ty = y / size;
+                const tx = x / size;
                 const i = (y * size + x) * 4;
-                // Top-left: bright white/lavender Ôćĺ Top-right: bright pink
-                // Bottom-left: deep purple Ôćĺ Bottom-right: teal/blue
-                const r = Math.round(
-                    255 * (0.9 - ty * 0.4) * (0.6 + tx * 0.4));
-                const g = Math.round(
-                    255 * (0.3 - ty * 0.2) * (0.4 - tx * 0.2));
-                const b = Math.round(
-                    255 * (0.9 - tx * 0.5) * (0.7 + ty * 0.2));
-                data[i] = Math.min(255, r);
-                data[i + 1] = Math.min(255, g);
-                data[i + 2] = Math.min(255, b);
+                // Studio-style gradient: bright top, dark bottom, soft horizon band
+                const horizon = 1.0 - Math.abs(ty - 0.45) * 2.2;
+                const horizonBand = Math.max(0, horizon) * 0.6;
+                const topLight = Math.max(0, 1.0 - ty * 1.8) * 0.9;
+                const bottomDark = Math.max(0, ty - 0.6) * 0.3;
+                // Subtle side gradient for directional feel
+                const sideGrad = 0.05 * Math.sin(tx * Math.PI);
+                const base = topLight + horizonBand - bottomDark + sideGrad;
+                const val = Math.round(Math.min(255, Math.max(0, base * 255)));
+                data[i] = val;
+                data[i + 1] = val;
+                data[i + 2] = Math.round(Math.min(255, val * 1.03)); // tiny cool tint
                 data[i + 3] = 255;
             }
         }
@@ -46,9 +46,8 @@ function useProceduralEnv() {
     }, [gl]);
 }
 
-/* ÔöÇÔöÇÔöÇ Logo mesh ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function LogoMesh({ mouse }: { mouse: React.MutableRefObject<[number, number]> }) {
-    const meshRef = useRef<THREE.Mesh>(null);
+/* ÔöÇÔöÇÔöÇ Static logo mesh ÔÇö no animation, no mouse tracking ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
+function LogoMesh() {
     const envMap = useProceduralEnv();
 
     const geometry = useMemo(() => {
@@ -59,86 +58,65 @@ function LogoMesh({ mouse }: { mouse: React.MutableRefObject<[number, number]> }
         const geo = new THREE.ExtrudeGeometry(shapes[0], {
             depth: 30,
             bevelEnabled: true,
-            bevelThickness: 12,   // thick bevel = smooth rounded edges like the reference
+            bevelThickness: 12,
             bevelSize: 10,
-            bevelSegments: 14,    // many segments = very smooth rounding
-            curveSegments: 24,    // smooth curves on the circular parts
+            bevelSegments: 14,
+            curveSegments: 24,
         });
         geo.center();
         return geo;
     }, []);
 
-    const rot = useRef<[number, number]>([0.3, -0.25]);
-
-    useFrame(() => {
-        if (!meshRef.current) return;
-        const [mx, my] = mouse.current;
-        rot.current[0] += (my * 0.7 - rot.current[0]) * 0.05;
-        rot.current[1] += (mx * 0.7 - rot.current[1]) * 0.05;
-        meshRef.current.rotation.x = rot.current[0];
-        meshRef.current.rotation.y = rot.current[1];
-    });
-
     return (
-        <Float speed={1.4} rotationIntensity={0.08} floatIntensity={0.4} floatingRange={[-0.2, 0.2]}>
-            <mesh ref={meshRef} geometry={geometry} scale={[0.055, -0.055, 0.055]}>
-                <meshPhysicalMaterial
-                    color="#e0d8ff"
-                    transparent
-                    opacity={0.78}
-                    metalness={0.1}
-                    roughness={0.0}
-                    iridescence={1.0}
-                    iridescenceIOR={2.2}
-                    iridescenceThicknessRange={[0, 900]}
-                    sheen={0.8}
-                    sheenColor="#d070ff"
-                    sheenRoughness={0.0}
-                    clearcoat={1.0}
-                    clearcoatRoughness={0.0}
-                    reflectivity={1.0}
-                    envMap={envMap}
-                    envMapIntensity={6.0}
-                    side={THREE.DoubleSide}
-                    depthWrite={false}
-                />
-            </mesh>
-        </Float>
+        <mesh geometry={geometry} scale={[0.04, -0.04, 0.04]} rotation={[0.3, -0.25, 0]}>
+            <meshPhysicalMaterial
+                color="#c8bfff"
+                metalness={0.05}
+                roughness={0.05}
+                iridescence={1.0}
+                iridescenceIOR={2.0}
+                iridescenceThicknessRange={[100, 800]}
+                sheen={1.0}
+                sheenColor="#c070ff"
+                sheenRoughness={0.05}
+                clearcoat={1.0}
+                clearcoatRoughness={0.02}
+                reflectivity={1.0}
+                envMap={envMap}
+                envMapIntensity={5.0}
+                side={THREE.DoubleSide}
+            />
+        </mesh>
     );
 }
 
-/* ÔöÇÔöÇÔöÇ Lights matching the reference: cool-key + pink-fill + purple-rim ÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function Scene({ mouse }: { mouse: React.MutableRefObject<[number, number]> }) {
+/* ÔöÇÔöÇÔöÇ Lights ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
+function Scene() {
     return (
         <>
             <ambientLight intensity={0.1} />
-            {/* Bright key ÔÇö cool white from upper-left */}
             <directionalLight position={[-3, 5, 5]} intensity={6} color="#ffffff" />
-            {/* Pink fill ÔÇö matches the pink glow in the reference */}
             <pointLight position={[4, -2, 3]} intensity={8} color="#ff4488" />
-            {/* Deep purple rim ÔÇö back-lights the shape */}
             <pointLight position={[-2, 4, -4]} intensity={10} color="#9922ff" />
-            {/* Cyan accent ÔÇö creates the teal edge you can see in the reference */}
             <pointLight position={[3, -4, -3]} intensity={5} color="#22eeff" />
-            {/* Warm top highlight */}
             <pointLight position={[0, 6, 2]} intensity={4} color="#ffccff" />
-
-            <LogoMesh mouse={mouse} />
+            <LogoMesh />
         </>
     );
 }
 
-/* ÔöÇÔöÇÔöÇ Public component: lazy-mount on scroll ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
+/* ÔöÇÔöÇÔöÇ Public component: static 3D logo, moves with card tilt ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
 export default function AovaLogo3D({
-    cardRef,
+    size = 100,
+    className = "",
 }: {
-    cardRef: React.RefObject<HTMLElement | null>;
+    cardRef?: React.RefObject<HTMLElement | null>;
+    size?: number;
+    className?: string;
 }) {
-    const mouse = useRef<[number, number]>([0, 0]);
     const wrapRef = useRef<HTMLDivElement>(null);
     const [visible, setVisible] = useState(false);
 
-    /* Activate WebGL only when card enters the viewport */
     useEffect(() => {
         const el = wrapRef.current;
         if (!el) return;
@@ -150,31 +128,17 @@ export default function AovaLogo3D({
         return () => io.disconnect();
     }, []);
 
-    /* Mouse-relative tracking on the card */
-    useEffect(() => {
-        const card = cardRef.current;
-        if (!card) return;
-        const onMove = (e: MouseEvent) => {
-            const r = card.getBoundingClientRect();
-            mouse.current = [
-                ((e.clientX - r.left) / r.width) * 2 - 1,
-                -(((e.clientY - r.top) / r.height) * 2 - 1),
-            ];
-        };
-        const onLeave = () => { mouse.current = [0, 0]; };
-        card.addEventListener("mousemove", onMove);
-        card.addEventListener("mouseleave", onLeave);
-        return () => {
-            card.removeEventListener("mousemove", onMove);
-            card.removeEventListener("mouseleave", onLeave);
-        };
-    }, [cardRef]);
-
     return (
-        <div ref={wrapRef} style={{ width: 100, height: 100, margin: "0 auto" }}>
+        <div
+            ref={wrapRef}
+            className={`pointer-events-none ${className}`}
+            style={{ width: size, height: size, margin: "0 auto" }}
+        >
             {visible && (
                 <Canvas
-                    camera={{ position: [0, 0, 15], fov: 38 }}
+                    camera={{ position: [0, 0, 18], fov: 32 }}
+                    dpr={[1, 1.5]}
+                    frameloop="demand"
                     gl={{
                         alpha: true,
                         antialias: true,
@@ -189,7 +153,7 @@ export default function AovaLogo3D({
                         gl.setClearAlpha(0);
                     }}
                 >
-                    <Scene mouse={mouse} />
+                    <Scene />
                 </Canvas>
             )}
         </div>
